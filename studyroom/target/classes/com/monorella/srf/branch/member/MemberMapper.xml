<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.monorella.srf.branch.member.MemberMapper">

	<select id="searchPageMemberList" parameterType="java.util.Map" resultType="com.monorella.srf.branch.dto.Member">
		SELECT
		 member_nm
		 , member_sex
		 , member_age
		 , member_seat_state
		FROM branch_member_tb WHERE #{so} LIKE CONCAT('%',#{sv},'%') ORDER BY member_cd DESC LIMIT #{currentPage}, #{pagePerRow}
	</select>

	<select id="searchMemberHap" parameterType="java.util.Map" resultType="com.monorella.srf.branch.dto.Member">
		SELECT *
		FROM branch_member_tb
		WHERE member_nm LIKE CONCAT('%',#{sv},'%') or member_seat_state LIKE CONCAT('%',#{sv},'%')
	</select>

	<select id="searchMemberList" parameterType="java.util.Map" resultType="com.monorella.srf.branch.dto.Member">
		SELECT
			 member_nm
			 , member_sex
			 , member_age
			 , member_seat_state
		FROM branch_member_tb
		WHERE #{so} LIKE CONCAT('%',#{sv},'%')
	</select>

	<select id="selectMemberList" parameterType="java.util.Map" resultType="com.monorella.srf.branch.dto.Member">
		SELECT
			member_cd
			, branch_owner_cd
			, staff_id
			, seat_cd
			, inout_num
			, member_nm
			, member_sex
			, member_tel
			, member_addr
			, member_date
			, member_regi_date
			, member_end_date
			, member_regi_path
			, member_age
			, member_seat_state
		FROM branch_member_tb
		ORDER BY inout_num DESC
		LIMIT #{beginRow}, #{pagePerRow}
	</select>	


	<select id="selectMemberCount" resultType="int">
        SELECT COUNT(*) FROM branch_member_tb
    </select>
	
	<insert id="insertMember" parameterType="com.monorella.srf.branch.dto.Member">
		INSERT INTO branch_member_tb(
			member_cd
			, branch_owner_cd
			, staff_id
			, seat_cd
			, inout_num
			, member_nm
			, member_sex
			, member_tel
			, member_addr
			, member_date
			, member_regi_date
			, member_end_date
			, member_regi_path
			, member_age
			, member_seat_state)
		VALUES(
			#{member_cd}
			, #{branch_owner_cd}
			, #{staff_id}
			, #{seat_cd}
			, #{inout_num}
			, #{member_nm}
			, #{member_sex}
			, #{member_tel}
			, #{member_addr}
			, now()
			, now()
			, now()
			, #{member_regi_path}
			, #{member_age}
			, #{member_seat_state}
		)
	</insert>		
</mapper>